# MongoDB connection string
DB_CONNECTION=mongodb://<username>:<password>@localhost:27017/library-management

# JWT secret for signing tokens
JWT_SECRET=your_jwt_secret

# Port for the server to listen on
PORT=5000